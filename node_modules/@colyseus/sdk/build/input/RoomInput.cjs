// Copyright (c) 2026 Endel Dreyer.
//
// This software is released under the MIT License.
// https://opensource.org/license/MIT
//
// colyseus.js@0.18.2
'use strict';

var tslib = require('tslib');
var schema = require('@colyseus/schema');
var input = require('@colyseus/schema/input');
var sharedTypes = require('@colyseus/shared-types');
var InputHandle = require('./InputHandle.cjs');
var RoomClock = require('../RoomClock.cjs');

var _RoomInput_instances, _RoomInput_room, _RoomInput_handle, _RoomInput_options, _RoomInput_encoder, _RoomInput_warnedIgnoredOptions, _RoomInput_ctorFromReflection, _RoomInput_stampRender, _RoomInput_stampReckon, _RoomInput_tickRate, _RoomInput_patchRate, _RoomInput_subSteps, _RoomInput_warnIfIgnored;
/**
 * Owns all per-room input state, kept off the {@link Room} class itself: the
 * schema constructor recovered from the JOIN handshake, the server-advertised
 * stamp/rate flags, and the lazily-created {@link InputHandle}.
 *
 * Created lazily by {@link Room} only when the room declares input (an
 * `INPUT_*` handshake section arrives) or the app calls `room.input(...)`.
 * Rooms that never use input (chat / lobby / turn-based) never allocate one.
 * @internal
 */
class RoomInput {
    constructor(room) {
        _RoomInput_instances.add(this);
        _RoomInput_room.set(this, void 0);
        // Impl type (not the public interface) so the TIMED decode can feed it the
        // server ack via the internal `ackInput()`.
        _RoomInput_handle.set(this, void 0);
        // First-call context, kept only to diagnose later room.input(options) calls
        // whose options are ignored (the handle is created once — first call wins).
        _RoomInput_options.set(this, void 0);
        _RoomInput_encoder.set(this, void 0);
        _RoomInput_warnedIgnoredOptions.set(this, false);
        /**
         * Schema constructor recovered via Reflection from the server's handshake
         * (the `INPUT_REFLECTION` tagged section). Falls back to `undefined` when
         * the server room didn't call `defineInput()`.
         *
         * Typed as `new () => any` (not `Schema`) on purpose — pinning to this
         * SDK's Schema type would clash with user instances coming from a
         * different copy of `@colyseus/schema` under multi-version installs.
         */
        _RoomInput_ctorFromReflection.set(this, void 0);
        /** `true` when the handshake advertised the SNAPSHOT-timeline stamp
         *  (`INPUT_OPTIONS`, `InputFlags.RENDER_TIME`). */
        _RoomInput_stampRender.set(this, false);
        /** `true` when the handshake advertised the RECKON-timeline stamp
         *  (`INPUT_OPTIONS`, `InputFlags.RECKON_TIME`). Set together with
         *  {@link #stampRender} ⇒ the 6-byte `[reckonTime][renderDelta]` prefix. */
        _RoomInput_stampReckon.set(this, false);
        /** Server-advertised fixed simulation/input step rate (Hz) from
         *  `defineInput({ tickRate })`. */
        _RoomInput_tickRate.set(this, void 0);
        /** Server-advertised state-patch interval (ms) = the reconcile cadence. */
        _RoomInput_patchRate.set(this, void 0);
        /** Server-advertised physics sub-steps per input tick
         *  (`setFixedTimestep(..., { subSteps })`). */
        _RoomInput_subSteps.set(this, void 0);
        tslib.__classPrivateFieldSet(this, _RoomInput_room, room, "f");
    }
    /** Snapshot cadence (ms), so {@link Room} can feed `clock.setPatchInterval`. */
    get patchRate() {
        return tslib.__classPrivateFieldGet(this, _RoomInput_patchRate, "f");
    }
    /**
     * Decode the `INPUT_REFLECTION` handshake section: install schema-builder
     * field descriptors on the reconstructed class so {@link InputEncoder} can
     * read its `$values` and emit non-empty packets, then cache the ctor.
     *
     * INPUT_REFLECTION is also the signal that the server called `defineInput()`
     * and will emit TIMED-prefixed state messages — so swap the default stub
     * clock for a real {@link RoomClockImpl} unless the user already replaced
     * `room.clock` with their own. Advances `it`.
     */
    applyReflection(buffer, it, sectionEnd) {
        const inputDecoder = schema.Reflection.decode(buffer.subarray(0, sectionEnd), it);
        schema.Reflection.makeEncodable(inputDecoder.state.constructor);
        tslib.__classPrivateFieldSet(this, _RoomInput_ctorFromReflection, inputDecoder.state.constructor, "f");
        if (tslib.__classPrivateFieldGet(this, _RoomInput_room, "f").clock === RoomClock.NULL_CLOCK)
            tslib.__classPrivateFieldGet(this, _RoomInput_room, "f").clock = new RoomClock.RoomClockImpl();
    }
    /**
     * Decode the `INPUT_OPTIONS` handshake section.
     * `[flags uint8][tickRate varint?][patchRate varint?][subSteps varint?]`,
     * varints in bit order. Advances `it`.
     */
    applyOptions(buffer, it) {
        const flags = buffer[it.offset++];
        tslib.__classPrivateFieldSet(this, _RoomInput_stampRender, (flags & sharedTypes.InputFlags.RENDER_TIME) !== 0, "f");
        tslib.__classPrivateFieldSet(this, _RoomInput_stampReckon, (flags & sharedTypes.InputFlags.RECKON_TIME) !== 0, "f");
        if (flags & sharedTypes.InputFlags.FIXED_TIMESTEP)
            tslib.__classPrivateFieldSet(this, _RoomInput_tickRate, schema.decode.number(buffer, it), "f");
        if (flags & sharedTypes.InputFlags.PATCH_RATE)
            tslib.__classPrivateFieldSet(this, _RoomInput_patchRate, schema.decode.number(buffer, it), "f");
        if (flags & sharedTypes.InputFlags.SUB_STEPS)
            tslib.__classPrivateFieldSet(this, _RoomInput_subSteps, schema.decode.number(buffer, it), "f");
    }
    /** Feed the server's last-processed input seq to the handle; returns the RTT
     *  sample (or `-1` before the handle exists / when the seq aged out). */
    ackInput(inputSeq) {
        return tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f") ? tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f").ackInput(inputSeq) : -1;
    }
    /** Reset the input round-trip on reconnect (see {@link Room} reconnection). */
    reset() {
        var _a;
        (_a = tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f")) === null || _a === void 0 ? void 0 : _a.reset();
    }
    /**
     * Lazily create and cache the per-room {@link InputHandle}; subsequent calls
     * return the same handle (options on later calls are ignored — a warning
     * fires once if they differ from the constructed handle's). Backs
     * `room.input(...)` — see that method for the full discovery/usage docs.
     */
    handle(options) {
        var _a;
        if (tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f")) {
            if (options !== undefined)
                tslib.__classPrivateFieldGet(this, _RoomInput_instances, "m", _RoomInput_warnIfIgnored).call(this, options);
            return tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f");
        }
        const Ctor = ((_a = options === null || options === void 0 ? void 0 : options.type) !== null && _a !== void 0 ? _a : tslib.__classPrivateFieldGet(this, _RoomInput_ctorFromReflection, "f"));
        if (!Ctor) {
            throw new Error("room.input(): no input schema available. The server room must call " +
                "`defineInput(YourInput)`, or you can pass `{ type: YourInput }` explicitly.");
        }
        const instance = new Ctor();
        // The InputEncoder always delta-encodes (no full-snapshot mode), so
        // there's nothing to configure here beyond mode/historySize.
        const encoder = new input.InputEncoder(instance, options);
        // The handle owns the input round-trip (send counter, RTT send-times, server-acked count);
        // the TIMED decode feeds it the ack and it produces RTT samples for the clock (see Room.onMessage).
        // `stampRender`/`stampReckon` are server-driven (INPUT_OPTIONS flags); `renderDelay` lets the app subtract its interp buffer.
        tslib.__classPrivateFieldSet(this, _RoomInput_handle, new InputHandle.InputHandleImpl(tslib.__classPrivateFieldGet(this, _RoomInput_room, "f"), instance, encoder, {
            stampRender: tslib.__classPrivateFieldGet(this, _RoomInput_stampRender, "f"),
            stampReckon: tslib.__classPrivateFieldGet(this, _RoomInput_stampReckon, "f"),
            renderDelay: options === null || options === void 0 ? void 0 : options.renderDelay,
            allowRewind: options === null || options === void 0 ? void 0 : options.allowRewind, // app gate: skip the stamp on inputs the server won't rewind
            tickRate: tslib.__classPrivateFieldGet(this, _RoomInput_tickRate, "f"),
            patchRate: tslib.__classPrivateFieldGet(this, _RoomInput_patchRate, "f"),
            subSteps: tslib.__classPrivateFieldGet(this, _RoomInput_subSteps, "f"),
        }), "f");
        tslib.__classPrivateFieldSet(this, _RoomInput_options, options, "f");
        tslib.__classPrivateFieldSet(this, _RoomInput_encoder, encoder, "f");
        return tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f");
    }
}
_RoomInput_room = new WeakMap(), _RoomInput_handle = new WeakMap(), _RoomInput_options = new WeakMap(), _RoomInput_encoder = new WeakMap(), _RoomInput_warnedIgnoredOptions = new WeakMap(), _RoomInput_ctorFromReflection = new WeakMap(), _RoomInput_stampRender = new WeakMap(), _RoomInput_stampReckon = new WeakMap(), _RoomInput_tickRate = new WeakMap(), _RoomInput_patchRate = new WeakMap(), _RoomInput_subSteps = new WeakMap(), _RoomInput_instances = new WeakSet(), _RoomInput_warnIfIgnored = function _RoomInput_warnIfIgnored(later) {
    var _a, _b, _c;
    if (tslib.__classPrivateFieldGet(this, _RoomInput_warnedIgnoredOptions, "f"))
        return;
    const handle = tslib.__classPrivateFieldGet(this, _RoomInput_handle, "f");
    const diffs = [];
    if (later.type !== undefined && later.type !== ((_a = handle.data) === null || _a === void 0 ? void 0 : _a.constructor))
        diffs.push("type");
    if (later.mode !== undefined && later.mode !== handle.mode)
        diffs.push("mode");
    // historySize is inert in reliable mode (for BOTH calls) — only compare when unreliable.
    if (later.historySize !== undefined && handle.mode === "unreliable"
        && later.historySize !== tslib.__classPrivateFieldGet(this, _RoomInput_encoder, "f").historySize)
        diffs.push("historySize");
    if (later.renderDelay !== undefined && later.renderDelay !== ((_b = tslib.__classPrivateFieldGet(this, _RoomInput_options, "f")) === null || _b === void 0 ? void 0 : _b.renderDelay))
        diffs.push("renderDelay");
    // Function identity is meaningless across call sites — compare presence only.
    if ((later.allowRewind !== undefined) !== (((_c = tslib.__classPrivateFieldGet(this, _RoomInput_options, "f")) === null || _c === void 0 ? void 0 : _c.allowRewind) !== undefined))
        diffs.push("allowRewind");
    if (diffs.length === 0)
        return;
    tslib.__classPrivateFieldSet(this, _RoomInput_warnedIgnoredOptions, true, "f");
    console.warn("@colyseus/sdk: room.input() options ignored — the input handle was already " +
        `created by an earlier call (first call wins). Differing: ${diffs.join(", ")}.`);
};

exports.RoomInput = RoomInput;
//# sourceMappingURL=RoomInput.cjs.map
