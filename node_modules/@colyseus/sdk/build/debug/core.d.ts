import type { Auth } from "../Auth.ts";
export declare const roomDebugInfo: Map<any, any>;
export declare const authInstances: Set<Auth<any>>;
export declare const preferences: {
    maxLatency: number;
    latencySimulation: {
        enabled: boolean;
        delay: number;
        jitter: number;
    };
    panelPosition: {
        position: string;
    };
};
export declare function loadPreferences(): void;
export declare function savePreferences(): void;
export declare function getDebugRoot(): ShadowRoot;
/**
 * The latency ramp: green (0) → yellow (half of `maxLatency`) → red (max).
 *
 * Drives both the logo's border ring and the network sliders' track gradient, which
 * must agree — a slider that reads amber above a green ring is just confusing.
 */
export declare function latencyRamp(value: number, max: number): {
    r: number;
    g: number;
    b: number;
};
export declare function getBorderColor(latencyValue: any, opacity: any): string;
export declare function applyPanelPosition(): void;
export declare const PREDICT_CONTAINER_ID = "colyseus-debug-predict-container";
/** True when the room panels + Predict overlay should be on screen right now. */
export declare function isOverlayVisible(): boolean;
export declare function isCompactRevealed(): boolean;
/** Show/hide the compact overlay content. The logo itself always stays put. */
export declare function setCompactRevealed(revealed: boolean): void;
/** The menu is opening or closing — on compact it displaces the drawer. */
export declare function setCompactMenuOpen(open: boolean): void;
export declare function applyOverlayVisibility(): void;
export declare function hidePanelsForSession(): void;
export declare function formatBytes(bytes: any): string;
export declare function getPanelStack(): HTMLElement;
/**
 * Anchor the stack and pick its axis.
 *
 * Desktop keeps the historical row along the anchored edge, with Predict floating
 * in the opposite corner. Compact turns the stack into a **drawer**: one full-width
 * scrollable column that also adopts the Predict card (see positionPredictContainer).
 * Two fixed stacks growing toward each other from opposite edges only worked while
 * both were collapsed — expanded, they meet in the middle of a short screen.
 *
 * Either way the first child (oldest panel) sits nearest the anchor corner.
 */
export declare function repositionDebugPanels(): void;
export declare function isPanelsHidden(): boolean;
