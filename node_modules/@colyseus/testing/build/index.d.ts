import './Room.ext.ts';
import { Server, type SDKTypes } from "@colyseus/core";
import { type ConfigOptions } from "@colyseus/tools";
import { ColyseusTestServer } from './TestServer.ts';
export declare function boot<ServerType extends SDKTypes = any>(config: ConfigOptions & ServerType, port?: number): Promise<ColyseusTestServer<ServerType>>;
export declare function boot(config: Server, port?: number): Promise<ColyseusTestServer<any>>;
export { ColyseusTestServer };
