const __cjsImportMetaUrl = require('node:url').pathToFileURL(__filename).href;
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/testing/src/TestServer.ts
var TestServer_exports = {};
__export(TestServer_exports, {
  ColyseusTestServer: () => ColyseusTestServer
});
module.exports = __toCommonJS(TestServer_exports);
var import_Room_ext = require("./Room.ext.cjs");
var import_core = require("@colyseus/core");
var import_sdk = require("@colyseus/sdk");
var httpie = __toESM(require("httpie"), 1);
var ColyseusTestServer = class {
  constructor(server) {
    this.server = server;
    const hostname = "127.0.0.1";
    const port = server["port"];
    this.sdk = new import_sdk.ColyseusSDK(`ws://${hostname}:${port}`);
    const httpEndpoint = `http://${hostname}:${port}`;
    this.http = {
      ["get"]: (segments, opts) => httpie.get(`${httpEndpoint}${segments}`, opts),
      ["post"]: (segments, opts) => httpie.post(`${httpEndpoint}${segments}`, opts),
      ["patch"]: (segments, opts) => httpie.patch(`${httpEndpoint}${segments}`, opts),
      ["delete"]: (segments, opts) => httpie.del(`${httpEndpoint}${segments}`, opts),
      ["put"]: (segments, opts) => httpie.put(`${httpEndpoint}${segments}`, opts)
    };
  }
  // Implementation
  async createRoom(roomName, clientOptions = {}) {
    const room = await import_core.matchMaker.createRoom(roomName, clientOptions);
    return this.getRoomById(room.roomId);
  }
  async connectTo(room, clientOptions = {}) {
    const client = await this.sdk.joinById(room.roomId, clientOptions);
    await client.waitForInitialState();
    return client;
  }
  // Implementation
  getRoomById(roomId) {
    return import_core.matchMaker.getLocalRoomById(roomId);
  }
  async cleanup() {
    await Promise.all(import_core.matchMaker.disconnectAll());
    await this.sdk.auth.signOut();
    const driver = this.server["driver"];
    if (driver) {
      await driver.clear();
    }
  }
  async shutdown() {
    await this.server.gracefullyShutdown(false);
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ColyseusTestServer
});
