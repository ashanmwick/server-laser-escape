const __cjsImportMetaUrl = require('node:url').pathToFileURL(__filename).href;
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src-backend/json-schema.ts
var json_schema_exports = {};
__export(json_schema_exports, {
  toJSONSchema: () => toJSONSchema
});
module.exports = __toCommonJS(json_schema_exports);
var optional = (name) => import(name).catch(() => void 0);
var vendorConverters = {
  zod: async (schema) => {
    var _a;
    return (_a = await optional("zod")) == null ? void 0 : _a.toJSONSchema(schema);
  },
  // zod < 4.2
  valibot: async (schema) => {
    var _a;
    return (_a = await optional("@valibot/to-json-schema")) == null ? void 0 : _a.toJsonSchema(schema);
  },
  // effect 3 converts directly; effect 4 upgrades the schema to the interface above
  effect: async (schema) => {
    var _a, _b, _c;
    const effect = await optional("effect");
    return (_c = (_a = effect == null ? void 0 : effect.JSONSchema) == null ? void 0 : _a.make(schema)) != null ? _c : toJSONSchema((_b = effect == null ? void 0 : effect.Schema) == null ? void 0 : _b.toStandardJSONSchemaV1(schema));
  }
};
async function toJSONSchema(schema) {
  var _a, _b;
  const standard = schema == null ? void 0 : schema["~standard"];
  if (!standard) {
    return null;
  }
  try {
    if (standard.jsonSchema) {
      return standard.jsonSchema.input({ target: "draft-2020-12" });
    }
  } catch (e) {
  }
  try {
    return (_b = await ((_a = vendorConverters[standard.vendor]) == null ? void 0 : _a.call(vendorConverters, schema))) != null ? _b : null;
  } catch (e) {
  }
  return null;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  toJSONSchema
});
