import { type Endpoint } from '@colyseus/core';
export interface OAuthEndpointsOptions {
    prefix?: string;
    cookieSecret?: string;
}
export declare function oauthEndpoints(opts?: OAuthEndpointsOptions): Record<string, Endpoint>;
