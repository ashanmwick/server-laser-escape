const __cjsImportMetaUrl = require('node:url').pathToFileURL(__filename).href;
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/auth/src/index.ts
var index_exports = {};
__export(index_exports, {
  Hash: () => import_Hash.Hash,
  JWT: () => import_JWT.JWT,
  anonymousEndpoint: () => import_endpoints.anonymousEndpoint,
  auth: () => import_auth.auth,
  confirmEmailEndpoint: () => import_endpoints.confirmEmailEndpoint,
  endpoints: () => import_endpoints.endpoints,
  forgotPasswordEndpoint: () => import_endpoints.forgotPasswordEndpoint,
  htmlTemplatePath: () => import_templates.htmlTemplatePath,
  loginEndpoint: () => import_endpoints.loginEndpoint,
  readTemplate: () => import_templates.readTemplate,
  registerEndpoint: () => import_endpoints.registerEndpoint,
  resetPasswordGetEndpoint: () => import_endpoints.resetPasswordGetEndpoint,
  resetPasswordPostEndpoint: () => import_endpoints.resetPasswordPostEndpoint,
  userdataEndpoint: () => import_endpoints.userdataEndpoint
});
module.exports = __toCommonJS(index_exports);
var import_express_jwt = require("express-jwt");
var import_core = require("@colyseus/core");
var import_JWT = require("./JWT.cjs");
var import_auth = require("./auth.cjs");
var import_Hash = require("./Hash.cjs");
var import_templates = require("./templates.cjs");
var import_endpoints = require("./endpoints.cjs");
var __frameworkDefaultOnAuth = import_core.Room.onAuth;
if (import_core.Room.onAuth === __frameworkDefaultOnAuth) {
  import_core.Room.onAuth = async function jwtDecodingOnAuth(token) {
    if (!token) {
      return true;
    }
    try {
      const decoded = await import_JWT.JWT.verify(token);
      const check = import_JWT.JWT.settings.revocationCheck;
      if (check) {
        const ok = await check(decoded);
        if (!ok) {
          return false;
        }
      }
      return decoded;
    } catch {
      return false;
    }
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Hash,
  JWT,
  anonymousEndpoint,
  auth,
  confirmEmailEndpoint,
  endpoints,
  forgotPasswordEndpoint,
  htmlTemplatePath,
  loginEndpoint,
  readTemplate,
  registerEndpoint,
  resetPasswordGetEndpoint,
  resetPasswordPostEndpoint,
  userdataEndpoint
});
